<?php

$input = '[1, 4, 2, 0]';

function stringToIntegerList(string $input): array {
    // kood tuleb siia

    return [];
}

//// check that the restored list is the same as the input list.
//print var_dump($list === [1, 4, 2, 0]); // should print "bool(true)"

